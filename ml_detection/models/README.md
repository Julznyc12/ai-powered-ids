# Models

Scaler files are included to ensure consistent feature preprocessing.

Trained Random Forest models are not included due to GitHub file size limitations.

To regenerate models:
- Run `ids_general_model.ipynb`
- Run `ids_ddos_model.ipynb`

These notebooks will recreate all trained models locally.
